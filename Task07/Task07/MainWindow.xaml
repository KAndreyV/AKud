<Window x:Class="Task07.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Category or SubCategory Viewer"
        Height="520"
        Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Width="220"
                    Height="40"
                    Margin="0,0,10,0"
                    Content="Показать субкатегории"
                    Click="ShowSubcategoriesButton_Click"/>

            <Button Width="220"
                    Height="40"
                    Content="Показать категории"
                    Click="ShowCategoriesButton_Click"/>
        </StackPanel>

        <TextBox x:Name="OutputTextBox"
                 Grid.Row="2"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 FontSize="16"
                 FontFamily="Consolas"/>
    </Grid>
</Window>